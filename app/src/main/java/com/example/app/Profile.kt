package com.example.app


class Profile(var name: String, var age: Int) {
    // Setter functions for name and age (Not actually required since properties have public setters)
    // But you can keep them for custom validation or additional logic if needed.

}
